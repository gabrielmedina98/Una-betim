/*Scanner input = new Scanner(System.in);
       
       
       3 - Faça um programa que leia um vetor com 10 elementos inteiros. Solicite ao usuário que informe um novo número, e verifique se este número encontra-se no vetor. 
       Caso positivo, imprima a(s) posição(ões) em que este número estiver no vetor.
       Caso contrário, exiba uma mensagem avisando ao usuário informando que o número não se encontra no vetor.
       
       System.out.println("------------------");
        int number, numero[] = new int[5];

        for (int i = 0; i < numero.length; i++) {
            System.out.println("Digite um numero de indice");
            numero[i] = input.nextInt();
        }
        System.out.println("Digite um numero");
        number = input.nextInt();

        int cont = 0;
        for (int i = 0; i < numero.length; i++) {
            if (numero[i] == number) {
                System.out.println("Elemento encontrado na posição" + i);
                cont++;
            }
            
        }
        
        if(cont==0){
        System.out.println("Elemento não encontrado");
        }*/
        
        
       /////////////////////////////////////////////////////
       Exercicio usando Vetor
        /*double temp, menortemp=9999;
        int menorMestemp=1;
         String mes[]= {"Janeiro","Fevereiro","Março","Abril","Maio","Junho"};
        
        for( int i=0; i<mes.length;i++){
            System.out.println("Digite a temperatura media de " +mes[i]+ " : ");
            temp = input.nextDouble();
        
            if(temp<=menortemp){
                menortemp=temp;
                menorMestemp = i;
            }
            
            
        }
        System.out.println("A menor temperatura " + menortemp);
        System.out.println("Mês da menor temperatura:" + mes[menorMestemp]);*/
